{
  "name": "open-xdownload",
  "private": true,
  "version": "0.1.8",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "dev:all": "./scripts/dev-all.sh",
    "build": "npm --prefix apps/web run build",
    "sync:web": "sh scripts/sync-webui.sh",
    "build:embedded": "npm run build && npm run sync:web",
    "server": "go run ./cmd/server",
    "test": "go test ./... && npm --prefix apps/web run build"
  }
}
